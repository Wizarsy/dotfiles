export PATH="${HOME}/.bin:${HOME}/.local/.bin:/opt:${PATH}"
export SSH_AUTH_SOCK="$(command fd "${XDG_RUTIME_DIR}" -name 'ssh-agent.*' -print0 -quit)"
export EDITOR="nvim"
export GOPATH="${HOME}/.go"
