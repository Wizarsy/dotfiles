#!/usr/bin/env bash

echo "Running Pre Commit script..."

dotfiles encrypt

if [[ "$(dotfiles config --get local.class)" == "wsl" ]]; then
  cp -vru /mnt/c/Users/Filipe/.config/wezterm/* "$HOME"/.config/wezterm/
fi
